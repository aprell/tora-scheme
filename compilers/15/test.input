(letrec

  ((even? (lambda (x)
            (if (zero? x)
                #t
                (odd? (sub1 x)))))

   (odd? (lambda (x)
           (if (zero? x)
               #f
               (even? (sub1 x))))))

  (even? 100)) ;; => t
